# PowerShell 配置快速参考卡片 v2.1

> 快速查阅常用命令和快捷键
> **v2.1 性能优化版** - 新增环境自检功能

---

## 🚀 快速安装

```powershell
# 1. 安装 Scoop
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
irm get.scoop.sh | iex

# 2. 安装核心工具
scoop install git oh-my-posh fastfetch eza bat zoxide fzf

# 3. 安装 PowerShell 模块
Install-Module PSReadLine, Terminal-Icons, PSFzf -Force

# 4. 部署配置文件
Copy-Item .\Microsoft.PowerShell_profile.ps1 $PROFILE -Force
. $PROFILE
```

---

## ⌨️ 快捷键速查

| 快捷键 | 功能 | 工具 |
|--------|------|------|
| `→` / `End` | 接受预测建议 | PSReadLine |
| `Ctrl+Space` | 显示所有预测 | PSReadLine |
| `F2` | 切换预测视图 | PSReadLine |
| `Ctrl+B` | 返回上一个目录 | zoxide |
| `Alt+Z` | 交互式目录选择 | zoxide |
| `Ctrl+F` | 模糊搜索文件 | PSFzf |
| `Ctrl+R` | 模糊搜索历史 | PSFzf |
| `Tab` | 自动补全 | PowerShell |
| `Ctrl+L` | 清屏 | PowerShell |

---

## 📝 命令别名

### 文件操作

| 别名 | 原命令 | 示例 |
|------|--------|------|
| `ls` / `dir` | `eza` | `ls -la` |
| `cat` | `bat` | `cat file.txt` |
| `grep` | `Select-String` | `grep "text" file.txt` |

### Git 操作

| 别名 | 原命令 | 示例 |
|------|--------|------|
| `g` | `git` | `g status` |
| `gst` | `git status` | `gst` |
| `gco` | `git checkout` | `gco main` |
| `gb` | `git branch` | `gb -a` |
| `gl` | `git log --oneline --graph --all` | `gl` |
| `gp` | `git pull` | `gp` |
| `gps` | `git push` | `gps` |

---

## 🛠️ 实用函数

| 函数 | 功能 | 示例 |
|------|------|------|
| `mkcd <path>` | 创建并进入目录 | `mkcd NewProject` |
| `Find-LargeFiles` | 查找大文件 | `Find-LargeFiles -TopN 20` |
| `Edit-Profile` | 编辑配置文件 | `Edit-Profile` |
| `Update-Profile` | 重新加载配置 | `Update-Profile` |
| `Get-SystemInfo` | 显示系统信息 | `Get-SystemInfo` |
| `Test-Internet` | 测试网络连接 | `Test-Internet` |
| `Test-Environment` | 🆕 一键检查环境 | `Test-Environment` |
| `Write-InfoLog` | 信息日志（带时间戳） | `Write-InfoLog "消息"` |
| `Write-WarnLog` | 警告日志（带时间戳） | `Write-WarnLog "警告"` |
| `Write-ErrorLog` | 错误日志（带时间戳） | `Write-ErrorLog "错误"` |

---

## 🔧 Zoxide 使用

```powershell
# 正常使用 cd，zoxide 自动记录
cd C:\Projects\MyApp

# 智能跳转（部分路径）
z myapp

# 交互式选择
zi app

# 返回上一个目录
z -

# 查看历史
zoxide query --list
```

---

## 📦 Scoop 常用命令

```powershell
scoop search <package>    # 搜索包
scoop install <package>   # 安装包
scoop update <package>    # 更新包
scoop update *            # 更新所有包
scoop list                # 列出已安装包
scoop uninstall <package> # 卸载包
scoop cleanup *           # 清理旧版本
```

---

## 🎨 Oh My Posh 主题

```powershell
# 查看所有主题
Get-ChildItem "$env:USERPROFILE/oh-my-posh-themes" -Filter *.omp.json

# 使用固定主题（编辑配置文件）
$theme = Get-Item "$themesPath/atomic.omp.json"

# 推荐主题
# - atomic.omp.json
# - powerlevel10k_rainbow.omp.json
# - night-owl.omp.json
# - jandedobbeleer.omp.json
```

---

## 🐛 常见问题快速修复

### 脚本无法执行

```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 中文乱码

```powershell
scoop install FiraCode-NF
# 然后在 Windows Terminal 设置中选择该字体
```

### 工具不生效

```powershell
# 检查工具是否安装
Get-Command eza, bat, zoxide

# 重新安装
scoop install eza bat zoxide

# 重新加载配置
Update-Profile
```

### PSReadLine 预测不显示

```powershell
Install-Module PSReadLine -Force -SkipPublisherCheck
```

---

## 📊 性能优化

### 减少启动时间

1. 使用固定主题（不随机）
2. 禁用 Fastfetch
3. 注释掉不需要的功能

### 清理历史

```powershell
Clear-History
Set-PSReadLineOption -MaximumHistoryCount 1000
```

---

## 🔗 快速链接

- **完整文档**: `PowerShell配置文档.md`
- **配置文件**: `$PROFILE`
- **Oh My Posh**: https://ohmyposh.dev/
- **Scoop**: https://scoop.sh/

---

## 💡 实用技巧

### 1. 快速进入常用目录

```powershell
# 在配置文件末尾添加
function cdp { Set-Location C:\Projects }
function cdd { Set-Location C:\Downloads }
```

### 2. 自动显示 Git 状态

```powershell
# 在配置文件末尾添加
if (Test-Path ".git") {
    git status -s
}
```

### 3. 备份配置文件

```powershell
Copy-Item $PROFILE "$PROFILE.backup.$(Get-Date -Format 'yyyyMMdd')"
```

### 4. 查看配置文件位置

```powershell
$PROFILE
```

### 5. 更新所有工具

```powershell
scoop update *
Update-Module PSReadLine, Terminal-Icons, PSFzf
```

---

**打印此页面作为速查表！** 📄

